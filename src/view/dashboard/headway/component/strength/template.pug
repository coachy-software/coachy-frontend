.row
  .col-sm-2
    .col-sm-8(v-for="(measurement, index) in headway.measurements")
      .form-group
        label.form-label {{measurement.name}}:
        p
          | {{measurement.value}}
  .col-sm-2
  .col-sm-8
    vue-c3.c3#chart-development-activity(:handler="handler")
