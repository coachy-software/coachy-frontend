.my-3.my-md-5
  .container
    .page-header
      h1.page-title {{this.$t('board.page_title')}}
    .board-container
      .board
        .lists-container
          section.list-container(v-for="(label, index) in this.labels")
            .list-header
              h4.text-center.mb-5.mt-2 {{label.name}}
              draggable(:options="{animation: 150}", :list="labels[index].tasks")
                Task(itemTitle="test xddddddddddddddddddddddddddddddddddddddddddddddddddddddddddd", itemDescription="123")
            .item-entry
              .input-group
                input.form-control(type='text', :placeholder="$t('board.new_task')")
                span.input-group-append
                  button.btn.btn-primary(type='button')
                    i.fe.fe-plus
